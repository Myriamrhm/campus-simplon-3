/*jshint esversion:6*/
/**
  * @param cond
  * @param cond
*/
const tablerFilter = function tablerFilter(element, data, searchedProp) {
  "use strict";
  const memory = data;
  return console.log(element, data, searchedProp)
  data.filter(function(elem) {
    return elem[prop] === val;
  });
};
