/*jshint esversion:6*/
/**
  * @param cond
  * @param cond
*/
const tablerSorter = function tablerSorter(tabler, prop, search) {
  "use strict";
  const memory = tabler;

  tabler.sort(function(elem) {
    return elem[prop] === val;
  });
};
